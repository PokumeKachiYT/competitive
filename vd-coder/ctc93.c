#undef _GLIBCXX_DEBUG
#pragma GCC optimize("Ofast,inline,unroll-loops")
#include <stdio.h>

int amount,num,i,j;
char output[1001];

int main() {
  FILE *in = stdin; //fopen("ctc93.inp","r");
  FILE *out = stdout; //fopen("ctc93.out","w");
  
  fscanf(in,"%d",&amount);

  j = 0;

  for (i = 0 ; i ^ amount ; ++i) {
    fscanf(in,"%d",&num);

    
  }


  return 0;
}
